[server]
hosts = 0.0.0.0:5232

[auth]
# No auth — only reachable via internal Docker network.
# External access goes through Traefik + Authentik forward auth.
type = none

[storage]
filesystem_folder = /data/collections

[logging]
level = warning
